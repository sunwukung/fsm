const errors = {
  ARGUMENTS: "invalid arguments",
  STATE_GRAPH: "invalid state graph",
  TRANSITION_TARGET: "invalid transition target"
};

function validateArguments(args, expectation) { };

function emitError() { };

export {errors};
